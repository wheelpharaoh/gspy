#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='gspy',
      version='1.0',
      description='gs rest api',
      author='wheelpharaoh',
      packages=find_packages(),
     )
